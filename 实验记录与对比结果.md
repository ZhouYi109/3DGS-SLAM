# 实验记录与对比结果

## 1. 记录规则

本文件只记录已经实际执行的实验、有效性和数值结果，不再包含部署步骤。
运行方法见 `工程架构与实验运行指南.md`。

编号规则：

```text
E000, E001, ...：实验主题
E003-A, E003-B, ...：同一受控变量下可直接对比的子实验
```

有效性标记：

- **正式**：配置、输入、退出状态和产物完整，可用于论文当前结论；
- **探索**：真实运行，但存在未冻结变量、无真值或样本不足；
- **无效**：契约错误或数据泄漏，只保留排障证据，不进入结论。

所有 R3LIVE HKU bag 均不包含外部 GNSS/RTK/mocap 位姿真值。除非另行引入真值，
不得报告 ATE/RPE；起终点误差和跨运行 RMSE 只表示内部一致性。

## 2. 实验索引

| 编号 | 主题 | 数据 | 状态 | 核心结论 |
|---|---|---|---|---|
| E000 | 基线冻结与首次完整运行 | R3LIVE | 探索 | 官方链和 FAST 主链可 headless 运行 |
| E001 | FAST 与 Coco 同机对比 | hku_park_00 | 探索 | FAST 前端和完整链更快，无外部真值 |
| E002 | Gaussian-LIC 原文 r0 复测 | hku_campus_seq_00 | 正式参考 | Coco 画质接近原文但略低 |
| E003 | FAST 画质偏差与相机契约 | hku_campus_seq_00 | 正式 | K=431 backend contract 消除约 4–5 dB 偏差 |
| E004 | 后端动态权重探索 | hku_campus_seq_00 | 探索 | 三重降权缩图但损失画质 |
| E005 | 冻结前端二因素权重消融 | 冻结五话题 | 正式 | 画质损失主要来自容量/点保留路径 |
| E006 | 在线语义投影与索引一致性 | 合成+短序列 | 正式功能验证 | densification/pruning 后索引一致 |
| E007 | Object Memory 完整闭环 | hku_campus_seq_00 | 探索 | 开放词汇查询闭环可用，语义覆盖仍低 |
| E008 | 三线程正确契约重复运行 | hku_campus_seq_00 | 正式系统基线 | deferred 基本不影响在线 Mapping |
| E009 | 在线语义 0.5/1/2 Hz | campus 前25秒 | 正式压力测试 | 单 GPU 频率越高，GPU 竞争越明显 |
| E010 | 0.5 Hz 三线程完整序列 | hku_campus_seq_00 | 正式 | 输入可在线接收，后端仍异步积压 |
| E011 | nuScenes-mini Prior 预训练 | nuScenes mini | 正式阶段结果 | 24D→14D 预训练与 TorchScript 可用 |
| E012 | R3LIVE 100-step Teacher v2 | 3 条 R3LIVE 序列 | 正式 | 100% candidate 配对，无输入泄漏 |
| E013 | held-out Prior 对 baseline | degenerate_seq_02 | 正式 | 2.77x 加速，新视角质量基本保持 |

## 3. E000 基线冻结与首次完整运行

### 3.1 目的

冻结可复现实验起点，确认远端以下链路可以构建和运行：

```text
FAST-LIVO2 -> Gaussian-LIC
Coco-LIC -> Gaussian-LIC
```

### 3.2 冻结内容

- FAST-LIVO2 R3LIVE 配置、轨迹、退化指标、退化分数和 3DGS packet；
- Coco-LIC/Gaussian-LIC 补丁；
- 官方 libtorch 路径；
- R3LIVE bag；
- PLY、GT、render、depth 和日志目录。

### 3.3 结论

`run_baseline_hku_001` 证明 FAST-LIVO2 可以产生轨迹、退化日志和后端 packet；
Gaussian-LIC 可以接收四路主题并增量加入 Gaussian。该实验主要用于工程冻结，
早期指标未满足统一相机契约和严格对照条件，不进入最终性能表。

## 4. E001 FAST-LIVO2 与 Coco-LIC 同机对比

数据：`hku_park_00.bag`，228.493 秒，3428 张图像。

| 指标 | FAST-LIVO2 + Gaussian-LIC | Coco-LIC + Gaussian-LIC |
|---|---:|---:|
| 前端累计 | 211.587 s | 459.961 s |
| 前端 ms/原始图像 | 61.72 | 134.18 |
| 前端吞吐 | 16.20 FPS | 7.45 FPS |
| 3DGS 内部计算 | 320.31 s | 247.02 s |
| 完整输出墙钟 | 541.480 s | 619.496 s |
| 完整等效吞吐 | 6.33 FPS | 5.53 FPS |
| Gaussian 接收帧 | 3420 | 2190 |
| 接收覆盖率 | 99.767% | 63.886% |
| Gaussian 数 | 1,178,299 | 1,820,783 |
| 训练 PSNR/SSIM/LPIPS | 17.07/0.536/0.430 | 16.93/0.595/0.379 |
| 同口径测试 PSNR | 17.558 | 15.667 |
| 同口径测试 SSIM | 0.46195 | 0.42526 |
| 同口径测试 LPIPS | 0.39260 | 0.41601 |

轨迹内部一致性：

| 指标 | FAST | Coco |
|---|---:|---:|
| 路径长度 | 212.273 m | 211.727 m |
| 起终点平移 | 0.023745 m | 0.158531 m |
| 起终点旋转 | 8.6695° | 8.4633° |

结论：FAST 前端平均速度约为并发 Coco 的 2.17 倍，但完整两条链都未达到严格实时。
本 bag 无外部真值，不能据此声称 FAST 的绝对定位精度更高。

## 5. E002 Gaussian-LIC 原文 r0 复测

### 5.1 Coco + Gaussian-LIC

数据：`hku_campus_seq_00.bag`，3034 张图像。

| 指标 | 实测 | Gaussian-LIC 原文 r0 | 差值 |
|---|---:|---:|---:|
| 训练 PSNR | 24.38 | 25.27 | -0.89 |
| 训练 SSIM | 0.788 | 0.805 | -0.017 |
| 训练 LPIPS | 0.253 | 0.212 | +0.041 |
| 新视角 PSNR | 23.77 | 24.52 | -0.75 |
| 新视角 SSIM | 0.766 | 0.785 | -0.019 |
| 新视角 LPIPS | 0.256 | 0.215 | +0.041 |

运行统计：

```text
Gaussian 接收 1956 帧
Coco RunBag 388.037 s
Gaussian Mapping 148.73 s
完整输出 531.328 s
最终 Gaussian 602,161
```

结论：当前公开代码与重构环境可进入原文同一画质区间，但仍低 0.75–0.89 dB。
当前公开 Coco 仅向后端提供彩色 LiDAR 点，而原文描述还包含在线视觉 SFM 点，
这是可能的实现差异之一。

## 6. E003 FAST 画质偏差与相机契约

### 6.1 诊断组

同一 `hku_campus_seq_00.bag`：

| 子组 | 后端权重 | 相机契约 | 训练 PSNR | 新视角 PSNR | Gaussian |
|---|---|---|---:|---:|---:|
| E003-A | 动态 | 旧 FAST 输出 + K=431 | 19.71 | 19.31 | 330,964 |
| E003-B | 固定1 | 旧 FAST 输出 + K=431 | 19.71 | 19.39 | 356,504 |
| E003-C | 固定1 | 旧 FAST 输出 + K≈646 | 18.68 | 18.28 | 393,227 |
| E003-D | 固定1 | 去畸变 backend contract + K=431 | 24.45 | 24.15 | 562,478 |

旧链路的问题不是数据集更换，也不是语义导致，而是：

- 图像、深度和点颜色没有遵守同一去畸变针孔模型；
- 前后端内参契约不一致。

### 6.2 修正链路完整结果

四路接口：

```text
image: bgr8, 640x512
depth: 32FC1, 640x512, m
pose: T_wc
points: world XYZRGB
四路最大时间戳差: 0
```

| 指标 | 修正 FAST | Coco 实测 | 原文 r0 |
|---|---:|---:|---:|
| 训练 PSNR | 24.45 | 24.38 | 25.27 |
| 训练 SSIM | 0.789 | 0.788 | 0.805 |
| 训练 LPIPS | 0.253 | 0.253 | 0.212 |
| 新视角 PSNR | 24.15 | 23.77 | 24.52 |
| 新视角 SSIM | 0.775 | 0.766 | 0.785 |
| 新视角 LPIPS | 0.255 | 0.256 | 0.215 |

运行统计：

```text
后端接收 3024/3034 帧
前端 LIO+VIO 170.49 s
Mapping/Adding/Extending 215.61/4.31/5.06 s
完整输出 370.895 s
等效 8.18 FPS
```

结论：修正相机契约后，FAST 与 Coco 画质基本持平；早期约 19 dB 的结果全部标为
无效画质结论，但保留用于说明契约错误。

## 7. E004 后端动态权重探索

受控条件：正确 K=431 contract；两组前端自适应权重均开启，只改变 Gaussian 后端
是否消费 `/weights_for_gs`。

| 指标 | 固定后端权重 | 动态后端权重 | 动态-固定 |
|---|---:|---:|---:|
| 训练 PSNR | 24.45 | 23.98 | -0.47 |
| 新视角 PSNR | 24.15 | 23.72 | -0.43 |
| 训练 LPIPS | 0.253 | 0.276 | +0.023 |
| 新视角 LPIPS | 0.255 | 0.278 | +0.023 |
| Gaussian 数 | 562,478 | 464,017 | -17.51% |
| Mapping | 215.61 s | 203.70 s | -5.52% |
| 完整输出 | 370.895 s | 359.868 s | -2.97% |

动态运行权重均值：

```text
visual=0.414
lidar=0.779
geometry=0.671
pose=0.857
```

当前三种后端作用同时存在：

```text
appearance = sqrt(w_rgb * w_pose)
keep_ratio = 0.5*w_geo + 0.5*w_pose
init_opacity = 0.05 + 0.10*w_geo
```

结论：动态权重确实生效，但外观、点保留和 opacity 同时降权造成欠拟合。
由于两组重新运行前端，E004 只作探索，严格归因以 E005 为准。

## 8. E005 冻结前端二因素权重消融

冻结输入：图像、深度、位姿、点云、权重各 3024 条；随机种子 `20260725`。

| 子组 | 外观权重 | 容量权重 |
|---|---|---|
| E005-A 固定 | 关 | 关 |
| E005-B 仅外观 | 开 | 关 |
| E005-C 仅容量 | 关 | 开 |
| E005-D 联合 | 开 | 开 |

### 8.1 质量与地图规模

| 指标 | 固定 | 仅外观 | 仅容量 | 联合 |
|---|---:|---:|---:|---:|
| 训练 PSNR | 24.41 | **24.48** | 24.08 | 24.15 |
| 训练 SSIM | 0.785 | **0.787** | 0.778 | 0.779 |
| 训练 LPIPS | 0.256 | **0.253** | 0.269 | 0.269 |
| 新视角 PSNR | 24.12 | **24.18** | 23.83 | 23.90 |
| 新视角 SSIM | 0.772 | **0.773** | 0.767 | 0.768 |
| 新视角 LPIPS | 0.258 | **0.255** | 0.271 | 0.271 |
| Gaussian 数 | 536,442 | 548,627 | 458,127 | 468,595 |

### 8.2 耗时

| 指标 | 固定 | 仅外观 | 仅容量 | 联合 |
|---|---:|---:|---:|---:|
| Mapping | 195.55 s | 198.60 s | 190.28 s | **187.86 s** |
| Mapping FPS | 15.46 | 15.23 | 15.89 | **16.10** |
| Mapping+Adding+Extending | 215.29 s | 219.96 s | 207.48 s | **202.80 s** |
| 在线后端实时因子 | 1.068 | 1.091 | 1.030 | **1.006** |

结论：

1. 仅外观权重没有损害画质，差异在小幅正向波动范围。
2. 容量权重使 Gaussian 减少约 14.6%，但新视角 PSNR 降低约 0.29 dB。
3. 联合组最快，主要收益来自容量路径；画质损失也主要来自容量/点保留/opacity。
4. 后续应分开调节保留率与 opacity，不再把它们视为一个不可拆权重。

## 9. E006 在线语义投影与索引一致性

### 9.1 合成 smoke

验证链路：

```text
semantic grid/object latent
 -> image/depth/pose 时间同步
 -> 逐点投影
 -> 新 Gaussian semantic index
 -> densification 复制
 -> pruning 同步删除
 -> sidecar 保存
```

单元测试和 C++/ROS smoke 均通过，语义 tensor 行数与最终 PLY 点数一致，
没有越界和错位。

### 9.2 真实短序列

真实 CLIP/SAM2 输出能够匹配 R3LIVE 图像并绑定到新增 Gaussian。
该阶段只验证数据通路，不将语义接入 Gaussian loss 或退化权重。

结论：功能闭环有效；缺少对象级真值，不能报告语义准确率。

## 10. E007 Object Memory 完整闭环

完整链：

```text
FAST-LIVO2
 -> Gaussian-LIC
 -> SAM2.1/CLIP Object Memory
 -> Gaussian object_id
 -> sidecar/query
```

代表性完整运行：

| 指标 | 结果 |
|---|---:|
| 真实 SAM2+CLIP 更新 | 221 |
| 推理均值/P95/最大 | 0.755/0.963/1.411 s |
| 对象内存 | 371 个对象，`(371,512)` FP16 |
| Gaussian 内对象特征库 | `(370,512)` FP16 |
| Gaussian 自身高维特征 | `(278164,0)` |
| 语义匹配/仅几何帧 | 217/2806 |
| 有 object ID 的 Gaussian | 11,337/278,164 |
| 语义覆盖率 | 4.08% |
| `a chair` Top-5 查询 | 成功 |

结论：对象级压缩存储和开放词汇查询闭环成立，但 4.08% 覆盖率低，
Top-5 查询成功不等于语义识别正确。

## 11. E008 三线程正确契约重复运行

配置：

```text
hku_campus_seq_00
r3live_paper + backend_contract
deferred object semantics
语义不进入 Gaussian 优化
```

| 指标 | 第一次 | 第二次 | 均值 |
|---|---:|---:|---:|
| Mapping | 205.41 s | 195.77 s | 200.59 s |
| 后端内部总耗时 | 215.80 s | 205.29 s | 210.55 s |
| 后端吞吐 | 14.01 FPS | 14.73 FPS | 14.36 FPS |
| 训练 PSNR | 24.16 | 24.02 | 24.09 |
| 新视角 PSNR | 23.89 | 23.77 | 23.83 |
| Gaussian | 395,078 | 375,717 | 385,398 |
| 完整任务吞吐 | 7.63 FPS | 8.07 FPS | 7.84 FPS |
| SAM2+CLIP/帧 | 0.490 s | 0.494 s | 0.492 s |

重复运行轨迹平移 RMSE `0.00899 m`，证明重新运行前端会造成毫米级波动；
严格后端消融应冻结五话题。

结论：deferred 语义不影响在线 Mapping，但语义结果也不是在线即时可用。

## 12. E009 在线语义频率预检

同一 campus 前 25 秒，367 个有效 packet：

| 指标 | 无语义 | 0.5 Hz | 1 Hz | 2 Hz |
|---|---:|---:|---:|---:|
| 实际更新 | 0 | 15 | 29 | 53 |
| 实际频率 | 0 | 0.574 | 1.113 | 2.033 Hz |
| 推理均值 | - | 0.611 | 0.611 | 0.584 s |
| Mapping | 7.77 | 9.66 | 12.07 | 14.34 s |
| Mapping 增量 | - | +24.3% | +55.3% | +84.6% |
| GPU 均值 | 13.83% | 26.04% | 40.67% | 46.79% |
| 新视角 PSNR | 25.08 | 25.22 | 24.93 | 25.08 |

结论：独立线程消除了软件反压，但没有消除同一 GPU 的物理竞争。
单卡默认 0.5 Hz，1 Hz 为增强档，2 Hz 只用于压力测试或双 GPU。

## 13. E010 0.5 Hz 三线程完整序列

数据：完整 `hku_campus_seq_00.bag`。

| 指标 | 结果 |
|---|---:|
| 前端 LIO+VIO | 170.13 s |
| 前端等效 | 56.20 ms/packet，17.79 FPS |
| Mapping | 218.74 s |
| 后端内部总耗时 | 230.88 s |
| 后端内部 | 76.35 ms/帧，13.10 FPS |
| bag 回放 | 203.36 s |
| bag 到 Gaussian 完成 | 363.94 s |
| 完整任务吞吐 | 7.79 FPS |
| 训练 PSNR/SSIM/LPIPS | 24.13/0.781/0.264 |
| 新视角 PSNR/SSIM/LPIPS | 23.87/0.769/0.266 |
| Gaussian | 484,326 |

语义与资源：

| 指标 | 结果 |
|---|---:|
| 语义更新/实际频率 | 110/0.490 Hz |
| 推理均值/P95/最大 | 0.713/0.881/0.985 s |
| 对象数 | 339 |
| GPU 均值/P95/峰值 | 57.61/95/98% |
| 显存均值/峰值 | 7537/8894 MiB |

结论：系统能按 1x 速度接收输入，但后端只有 13.10 FPS，bag 结束后仍需清空队列，
因此只能称“在线输入、异步建图”，不能称严格实时。

## 14. E011 nuScenes-mini Prior 预训练

数据：

```text
官方 v1.0-mini.tgz = 4,167,696,325 bytes
205 shards
618,606 points
train 489,643
validation 128,963
```

| 指标 | 结果 |
|---|---:|
| 模型 | 24D→14D，hidden 64，3 层 |
| epoch/batch | 30/8192 |
| 初始/最终训练 loss | 0.314233/0.195981 |
| best epoch | 12 |
| best validation loss | 0.278804 |
| CUDA smoke | `(1024,24)->(1024,14)` 全有限 |

产物：

```text
best.pt SHA256
22a5ae77286eeeb60e04c8a66bff1d2248c4f7c03f037733959d4a0a45c8cc47

TorchScript SHA256
3c0a4d81ef9bf797fef63e92e81580446d97595a1757b02a8e3cbbc047b31669
```

结论：完成几何预训练，但 nuScenes 阶段 mean/color 残差为零监督，
最终参数能力必须由 R3LIVE Teacher 蒸馏补齐。

## 15. E012 R3LIVE 100-step Teacher v2

### 15.1 契约故障与无效运行

首次 Teacher 运行虽然 YAML 为 100-step，但 runner 无条件覆盖为 20-step。
campus、degenerate_01、park 日志均确认 `residual_optimization_iters=20`。

处理：

- 修复 runner 默认值；
- Teacher smoke 明确检查 100-step；
- 旧 shard、manifest、checkpoint 和模型写入
  `INVALID_TEACHER_CONTRACT.txt`；
- 旧结果不进入训练和论文。

### 15.2 正式 Teacher

| 序列 | 用途 | 帧/KF | Mapping | 训练 PSNR | 新视角 PSNR | 配对高斯 |
|---|---|---:|---:|---:|---:|---:|
| campus | train | 3019/604 | 263.67 s | 24.42 | 24.10 | 403,958 |
| degenerate_01 | train | 1699/340 | 121.71 s | 22.42 | 22.01 | 215,713 |
| park | validation | 3419/684 | 334.04 s | 18.76 | 17.89 | 616,050 |

三序列配对率均为 100%，输入和目标全有限。

```text
train = 619,671 Gaussian
validation = 616,050 Gaussian
manifest = 21 shards
manifest SHA256 =
81c1da3b582422eca18208f778edc9acd27439a9bcf90ea071de207d6bf3a0c2
```

蒸馏 best epoch 为 1：

```text
validation_loss=2.040625
best.pt SHA256=
3119077ee47d2c5bc5421bd8b0bc6fafb4ccf00c84a74ba82f6c89525c0167b9
TorchScript SHA256=
3c2f39e2d7a68be7c6ece3d828ad1405c200fb1e713ad034d88b51e14be37ff0
```

结论：Teacher 数据有效，但 best epoch 过早，表明跨场景蒸馏容易过拟合。

## 16. E013 held-out Prior 20-step 对 baseline 100-step

测试序列：`degenerate_seq_02`，未生成或使用 Teacher。

### 16.1 运行统计

| 指标 | Prior 20-step | Baseline 100-step |
|---|---:|---:|
| 帧/KF | 1979/396 | 2014/403 |
| Mapping | 60.47 s | 170.61 s |
| Mapping/帧 | 30.56 ms | 84.71 ms |
| Mapping 吞吐 | 32.73 FPS | 11.80 FPS |
| Adding/Extending | 12.85/12.24 s | 11.27/11.98 s |
| GPU 均值/P95/峰值 | 23.44/60/74% | 40.34/85/93% |
| 显存均值/峰值 | 6832/7308 MiB | 6714/7458 MiB |
| 内置训练 PSNR | 21.19 | 24.20 |
| 内置新视角 PSNR | 20.21 | 20.25 |

### 16.2 公共源帧公平评估

两次 ROS 接收帧数不同，运行内 `test_XXXX.jpg` 不能直接按文件名对齐。
用 GT JPEG SHA256 对齐得到 1070 个公共新视角帧：

| 指标 | Prior | Baseline | Prior-Baseline |
|---|---:|---:|---:|
| PSNR | 20.4060 | 20.4794 | -0.0734 dB |
| SSIM | 0.81999 | 0.80370 | +0.01629 |
| LPIPS | 0.29784 | 0.30879 | -0.01095 |

公共帧 JSON SHA256：

```text
4f6049ee2dd6e77bb2a610999a289a3f8410e5100333bdafa2ca39e22b86cd34
```

结论：

1. Mapping 加速 `2.77x`，回放期 GPU 均值降低 16.9 个百分点。
2. 公共新视角 PSNR 只下降 0.073 dB，SSIM/LPIPS 更好。
3. 训练视角 PSNR 明显下降，第一版不能宣称全画质无损。
4. 下一步做 20/30/40-step、低学习率、冻结前层和 source-balanced sampling。

## 17. 无效或仅排障的结果

以下结果不得放入论文主表：

| 记录 | 原因 |
|---|---|
| 旧 FAST 约 19 dB 完整运行 | K≈646/K=431 及去畸变契约错配 |
| 旧三线程约 19 dB 运行 | 继承错误相机配置，只能说明调度功能 |
| 首次 R3LIVE Teacher/蒸馏/Prior | runner 把 100-step Teacher 覆盖为 20-step |
| 无真值 bag 上的“ATE/RPE” | 数学上没有外部参考，只能记录闭环/跨运行一致性 |
| 不冻结前端的语义 A/B | 前端轨迹和权重存在重复运行波动，不能做因果归因 |

## 18. 下一轮实验编号

预留：

| 编号 | 实验 |
|---|---|
| E014 | held-out Prior 20/30/40-step Pareto |
| E015 | 无 Prior 20-step 与 Prior 20-step |
| E016 | 低学习率/冻结前层/source-balanced 蒸馏 |
| E017 | 带外部真值数据集的 ATE/RPE 联合实验 |
| E018 | per-Gaussian 不确定性均值/协方差消融 |
| E019 | Gaussian-to-Object 概率关联与对象检索指标 |

---

维护规则：新实验必须先分配编号；同一变量的重复运行追加到原编号下并报告均值和方差，
不得为每次重跑新建文档。
